/**
 * server.js — Kept for backward compatibility.
 * The app config has been split into app.js + index.js.
 * New code should require('./app') for the Express app.
 */
module.exports = require("./app");
